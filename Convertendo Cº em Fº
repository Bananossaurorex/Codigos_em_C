#include <stdio.h>
#include <stdlib.h>
#include <locale.h>
int main(){
    setlocale(LC_ALL,"");
    float  celsius,conversorf=0;
    
    printf("Digite a temperaura em celsius: ");
    scanf("%f",&celsius);
    
    conversorf = (celsius*1.8)+32;
    printf("Graus em F: %.1f",conversorf);
}
